# 10 小时 PLD 冲刺交付规划

## 一 冲刺目标

本次不按学习阶段推进，也不要求本人从零编写全部代码。唯一目标是在 10 小时内留下可以复现、可以解释、可以演示的交付物。

目标平台固定为 PYNQ-Z2，器件为 XC7Z020-1CLG400C。所有综合、实现、资源和时序结论统一使用该目标，不在冲刺过程中切换器件。

默认交付边界：围绕现有的 `3×3 窗口 → 9 路点积 → 12 通道累加`模块，完成仿真证据、资源时序证据和复现说明。除非团队已经有稳定工程，不在本轮承诺 MIG、MicroBlaze、HDMI、网络接收、完整 FSRCNN 或整板视频链路。

## 二 本人分工

角色名称：RTL 验证与证据负责人。

负责：

1. 接收队友冻结的 RTL、接口和测试输入。
2. 运行现有 testbench，补充少量定向用例和自动 PASS/FAIL 检查。
3. 核对 signed、位宽扩展、valid 延迟、空拍和分组清零。
4. 在团队确定的目标器件和时钟约束下运行综合或实现。
5. 整理可提交的波形、日志、资源表、时序表和运行命令。

不负责：

- 从零搭建 MIG、MicroBlaze、AXI DMA、HDMI 或网络视频链路；
- 独立承担模型重构、训练、量化策略和 Python 黄金模型；
- 对未经实测的数据宣称板上帧率、最高频率或端到端性能。

## 三 10 小时安排

### 0:00 至 0:40 范围冻结

- 三人确认目标板卡、目标器件、Vivado 版本和最终要展示的功能。
- 确认队友提供的 RTL、权重或测试向量版本。
- 冻结接口、位宽、期望输出和完成标准。

产出：一页范围清单。若上述信息没有确认，不开始板级联调。

### 0:40 至 2:00 仓库与基线复现

- 整理 `rtl/`、`tb/`、`scripts/`、`docs/` 和 `results/`。
- 运行队友当前基线，记录环境、命令、报错和原始结果。
- 保留一个能随时回退的基线提交。

产出：基线 commit、第一份可复现命令和问题清单。

### 2:00 至 4:20 功能验证

- 运行窗口、点积、跨通道累加或团队实际采用模块的 testbench。
- 覆盖正数、负数、零、空拍、复位和分组边界。
- 优先使用自检查 testbench；波形只用于定位和展示。

产出：PASS 日志、代表性波形、失败用例修复记录。

### 4:20 至 6:20 联合验证

- 按冻结接口连接本轮真正要演示的最小数据通路。
- 核对模块间 valid 和数据延迟，不临时扩展到完整网络。
- 若联合集成超过 60 分钟仍无进展，立即退回“两个已验证模块加清楚接口说明”的保底交付。

产出：最小联合 testbench 或两个独立模块的完整验证包。

### 6:20 至 7:50 综合与时序证据

- 在已确定器件上运行综合；时间允许再运行实现。
- 记录 LUT、FF、DSP、BRAM、时钟约束、WNS 和报告边界。
- 不根据一次 WNS 反推最高频率，不把 OOC 报告写成板级签核。

产出：资源表、时序表、原始报告和截图。

### 7:50 至 9:10 交付包装

- 清理运行步骤，确保新目录下能按 README 重跑。
- 为波形和报告写图注；区分已完成、未完成和待集成。
- 整理一分钟演示顺序：输入、处理、PASS、资源、限制。

产出：README、接口说明、演示素材和已知限制。

### 9:10 至 10:00 冻结与缓冲

- 只修复影响演示或复现的问题，不再增加功能。
- 由另一位队友按 README 复跑一次关键命令。
- 提交最终 commit，推送远程，记录 commit ID。

产出：可交付版本和故障预案。

## 四 保底与升级边界

保底交付：两个或以上核心模块自检查仿真通过，加资源时序报告和复现说明。

目标交付：最小数据通路联合仿真通过，真实或约定测试向量自动比对正确。

只有在目标交付提前完成时，才尝试板卡联调、AXI 封装或视频链路。

## 五 对话组织

10 小时内默认只使用一个主对话，集中处理仓库、RTL、验证、报告和最终交付，避免多个对话同时修改同一工作区。

仅在以下情况下另开一个对话：代码已经冻结，需要独立整理答辩文案或演示材料；或者出现与主线完全独立、耗时较长的板卡问题。新对话不与主对话并行修改源码，并通过 Git commit 交接。

当前不建议拆成三个对话。上下文同步和合并冲突的成本可能超过收益。
