# 成员 C 十小时交付规划

## 一 冻结目标

目标平台为 PYNQ-Z2，器件为 XC7Z020-1CLG400C。输入为 640×360 单通道 Y，输出为 1280×720 单通道 Y。

本轮不实现原生 FSRCNN。冻结网络为三层 3×3 卷积加 2 倍子像素重排：

- L1：1 输入通道到 8 输出通道，PReLU；
- L2：8 输入通道到 16 输出通道，PReLU；
- L3：16 输入通道到 4 输出通道；
- L4：4 通道按 2×2 子像素重排为 1 通道高清输出。

权重与激活使用 INT8，卷积累加使用 INT32。成员 C 不负责训练，但必须能读取成员 B 交付的量化参数和测试向量。

## 二 成员 C 正确职责

成员 C 负责加速器 HLS 实现与 PYNQ 侧驱动，不以现有 Verilog 模块代替 HLS 交付。

必须交付：

1. AXI4-Stream 数据接口与 AXI4-Lite 寄存器规格。
2. 行缓存、滑窗、三层卷积、PReLU 和子像素重排的 HLS 源码。
3. 用于早期自测的 Python 或 NumPy 定点参考实现。
4. C 仿真与成员 B 黄金模型的逐层比对结果。
5. HLS 综合配置、脚本、约束和综合报告。
6. PYNQ Python 驱动，包括 overlay 加载、数据搬运、启动、轮询和结果读取。
7. 联调记录、降级演示材料和汇报稿初稿。

现有 Verilog 窗口、点积和累加器保留为结构参考与既有能力证据，不属于任务书规定的 O-7 HLS 最终交付。

## 三 依赖关系

成员 A 负责板卡、base overlay、Block Design、位流和板上运行。成员 C 向 A 交付 HLS IP、接口规格、综合脚本和驱动代码。

成员 B 负责模型、训练、INT8 量化、黄金模型和测试向量。成员 C 可以先用固定小数据自测，但最终位精确报告依赖 B 的量化规则和测试向量。

H0.5 前必须由三人冻结：网络结构、数据布局、量化格式、寄存器映射和文件命名。冻结后不再修改接口。

## 四 成员 C 时间盒

### H0 至 H0.5 接口冻结

- 完成 AXI4-Stream 与 AXI4-Lite 接口草案。
- 确定张量排列、权重排列、字节序、边界处理和子像素输出顺序。
- 把寄存器偏移和位定义写入共享规格。

产出：接口规格与寄存器表。

### H0.5 至 H1.5 可编译骨架

- 建立 Vitis HLS 工程与命令行脚本。
- 建立顶层函数、数据类型、AXI pragma 和三层处理框架。
- 建立 Python 定点参考脚本的输入输出格式。

产出：可编译 HLS 骨架和参考脚本骨架。

### H1.5 至 H3 HLS 核心实现

- 实现流式行缓存与 3×3 窗口。
- 实现三层多通道卷积、PReLU 与 INT32 累加。
- 实现 4 通道到 2×2 输出块的子像素重排。
- 使用固定权重与小尺寸图像完成第一轮自测。

产出：HLS 源码与基础测试平台。

### H3 至 H5 C 仿真与综合

- 先用成员 C 自建定向向量验证零值、单位权重、正负数和边界。
- 收到成员 B 数据后进行逐层比对，记录允许误差与首个错误位置。
- 运行 C 综合，导出 IP、资源估算、时序估算和综合日志。

产出：C 仿真报告、HLS IP 和综合报告。

若 H5 前全网络 C 仿真未通过，立即降级为单层或缩小尺寸的位精确演示，不再扩展功能。

### H5 至 H7 PYNQ 驱动

- 编写 overlay 加载与寄存器访问代码。
- 编写 DMA 缓冲分配、输入发送、结果读取和超时处理。
- 提供无需板卡即可检查的参数校验和数据打包单元测试。

产出：PYNQ Python 驱动和调用示例。

### H7 至 H8.5 联调支持

- 向成员 A 交付 IP、驱动、寄存器表和最小测试向量。
- 根据板上现象定位接口、DMA、地址映射或数值问题。
- 若 overlay 未通，立即转入三级演示：C 仿真输出对比 Python 黄金模型，加综合报告。

产出：调试记录或三级演示闭环。

### H8.5 至 H10 交付冻结

- 停止增加功能，整理复现命令、报告和截图。
- 汇总 HLS 资源、时序、仿真误差和已知限制。
- 编写成员 C 汇报内容，并为板卡失败准备离线演示材料。

产出：成员 C 全部代码、报告和汇报稿。

## 五 成员 C 降级顺序

1. 完整三层 HLS 加速器、板上单帧运行和 Jupyter 展示。
2. 完整三层 HLS C 仿真与综合通过，板上只完成 IP 加载或缩小尺寸测试。
3. 单层或缩小尺寸 HLS 位精确通过，加资源时序报告。
4. 现有 Verilog 模块只作为最后的既有能力证据，不能冒充三层 HLS 网络。

## 六 对话组织

成员 C 的 HLS、参考模型、接口和驱动高度耦合，默认全部放在当前一个主对话中完成。十小时内不拆成多个并行对话，避免接口版本不一致。

成员 A 和成员 B 可以各自使用独立对话推进自己的任务，但必须通过 Git 文件和冻结规格与成员 C 对接。若代码冻结后需要专门整理汇报材料，可再顺序开启一个文档对话，不与源码修改并行。
