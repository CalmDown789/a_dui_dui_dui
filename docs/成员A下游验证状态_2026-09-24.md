# 成员 A 下游验证状态说明

> 日期：2026-09-24  
> 本文件只记录成员 A 冻结交付物在下游验证中的使用情况，不修改成员 B、成员 C 的 RTL、约束、实现脚本或板级工程，也不把下游实验结果改写为成员 A 的验收结果。

## 1. 冻结版本与来源

成员 A 的 FPGA 模型交付保持以下冻结状态：

- 分支：`member-a`；
- 正式标签：`member-a-v1.0.1`；
- 标签提交：`081df91bb4ba962b736efb7ab40d258751fc4883`；
- 全尺寸整数 Golden 首次补齐提交：`98c82f3`；
- 模型：`d=16 / s=8 / m=1 / c=16`；
- 输入：`960×540`、8-bit Y；
- 输出：`1920×1080`、8-bit Y；
- 输出头：原生稠密 `5×5 Conv 16→4 + PixelShuffle×2`。

`98c82f3` 与 `081df91` 不是两套模型：前者引入全尺寸整数 Golden，后者在不改变模型、权重和 Golden 的前提下补充发布治理、来源和交付清单说明。

## 2. 权威整数数据

全尺寸权威输出：

```text
artifacts/full_integer_golden/output_1920x1080_y_u8.bin
```

- 大小：`2,073,600` 字节；
- CRC32：`87d353f1`；
- SHA-256：`be8e576beea1632e6ee8257ba39a92c9c7950e9ae90b202bd240677e2d85504e`。

C 侧输入 ROM：

```text
artifacts/full_integer_golden/input_rom_2p19_u8.mem
```

- 行数：`524,288`；
- 前 `518,400` 字节为有效输入，末尾 `5,888` 字节为 `00`；
- SHA-256：`f15e360bd0d5c3fb1ebd5e85cec32cafaf125c723890c39cf514301c064634c9`。

核心模型与量化文件哈希：

- FP32 checkpoint：`bb2ee7a2766185bb24e6fc16119db0ad7a69c8f1c4293a1ed0ceae0a142997c5`；
- `quant_params.json`：`f2a9f20ca6d51f4f2902e6e1b5e6bdb7632f62981930101b0aaeb0994351b77a`；
- 整数交付压缩包：`630ec68c56ae27267359d8fff98a9e40537eade33a39706fbba6cf6a9b457f74`。

## 3. 下游复用证据

截至本文日期，成员 B/C 已使用与上述哈希对应的成员 A 权重、量化参数、输入 ROM 和整数 Golden 完成以下下游验证：

- 四组 `96×54` 小尺寸测试向量；
- ready/valid 反压场景；
- `960×540 → 1920×1080` 全尺寸整数网络仿真；
- 输入 `518,400/518,400` 字节；
- 输出 `2,073,600/2,073,600` 字节；
- 最终输出逐字节 `mismatch=0`；
- 未知值 `X=0`；
- `stripe_last=17`、`frame_last=1`、`done=1`；
- 输出停顿保持规则违例为 `0`。

相关证据分别出现在下游的 `c-side-latest` 和 `member-b-2025-2-bc-trial` 分支。XSim 2022.2 的正式复现采用 `xelab -O0`；成员 B 的 Vivado/XSim 2025.2 实验也采用 `-O0`。这些结果说明下游 RTL 能够复现 A 的冻结整数合同，但不等于 bitstream、实体板图像或实时帧率已经验收。

## 4. 36-bit 内部累加器与 A 合同

成员 A 的对拍合同继续规定：

- 权重为 INT8；
- 中间激活为 INT16；
- 偏置、卷积累加语义及导出的逐层累加参考为 INT32；
- PReLU、Q31 重定量化、舍入和饱和规则保持冻结。

成员 B 在硬件内部使用 36-bit 保护累加器，属于实现 guard bits。只要进入 A 规定的量化边界时执行相同的截位、舍入、饱和和重定量化，并与 A 的逐层向量及最终 Golden 一致，就不需要修改 A 的权重、量化参数、逐层向量或全尺寸 Golden。

不能反向修改 A Golden 去迁就 RTL 输出；若下游重新出现差异，应先固定 RTL、ROM、量化参数、工具版本和文件哈希，再定位第一处不一致。

## 5. 算力数字的使用边界

成员 A 模型的确定性计算量为：

- `1.4681088 G MAC/帧`；
- 30fps 对应 `44.043264 GMAC/s`。

任务书中的 `133.2 GMAC/s` 和约 `3.02×` 余量依赖 DSP 打包、目标频率和利用率假设，只能称为条件性理论估计。它们不是板上实测吞吐，也不能单独证明 30fps。

下游的时钟收敛、周期数、bitstream、接口等待和真实帧率属于成员 B/C 的实现与板级证据，不改变成员 A 的模型计算量，但也不能由成员 A 的理论 MAC 数反向保证。

## 6. 职责边界与当前结论

成员 A 当前无需重新训练、重新量化或重新生成 Golden。以下文件保持冻结：

- FP32 checkpoint；
- INT8 权重、INT32 偏置和 Q1.15 PReLU 参数；
- `quant_params.json`；
- 四组逐层整数测试向量；
- 全尺寸输入 ROM 与整数 Golden；
- Set5 指标、训练日志和文件哈希。

成员 A 不在本分支内维护 RTL、Vivado 工程、XDC、bitstream 或板测数据。个人研究分支中的原论文 FSRCNN 复现和软件视频演示属于对照研究，不替换本分支冻结的 FPGA 候选模型。

当前结论：**成员 A 交付保持有效；下游位精确验证进一步证明了交付物可被 RTL 正确消费，但完整 FPGA 系统仍需由 B/C 完成时序、bitstream 和实体板验收。**
